const Footer = () => {
    return (
        <footer className="bg-brand-black-100 text-brand-gray py-2 flex justify-center items-center">
            &copy; {new Date().getFullYear()} #VANLIFE
        </footer>
    );
};

export default Footer;
